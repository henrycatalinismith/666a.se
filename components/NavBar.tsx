'use client'
import { clsx } from 'clsx'

export default function NavBar() {
  return (
    <header
      className={clsx(
        'relative',
        'sticky',
        'top',
        'border-b',
        'bg-background/60'
      )}
    >
      <div className={clsx('container', 'flex h-14', 'items-center')}>666a</div>
    </header>
  )
}
