<footer class="bg-white">
  <div class="mx-auto max-w-7xl overflow-hidden px-6 py-20 sm:py-24 lg:px-8">
    <nav class="-mb-6 columns-2 sm:flex sm:justify-center sm:space-x-12" aria-label="Footer">
      <% @policies.each do |policy| %>
        <div class="pb-6">
          <a href="<%= policy[:href] %>" class="text-sm leading-6 text-gray-600 hover:text-gray-900">
            <%= t(policy[:text]) %>
          </a>
        </div>
      <% end %>
    </nav>

    <div class="mt-10 flex justify-center space-x-10">
      <a href="#" class="text-gray-400 hover:text-gray-500">
        <img class="h-8 w-auto" src="/logo-128.png" alt="">
      </a>
    </div>

    <p class="mt-10 text-center text-xs leading-5 text-gray-500">&copy; <%= Date.today.strftime("%Y") %> 666a</p>
  </div>
</footer>
